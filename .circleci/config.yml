version: 2.1

workflows:
  test-deploy:
    jobs:
      - test

jobs:
  test:
    docker:
      - image: cimg/node:18.9.0
    steps:
      - checkout
      - run: 
          name: Install dependencies
          command: npm Install
      - run:
          name: Run Tests
          command: npm run test

